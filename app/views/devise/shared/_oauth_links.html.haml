- icons = { facebook: 'fa-facebook-official' }

- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to localized_omniauth_path(provider), class: 'general-login-icon' do
      %i.fa{ class: icons[provider] }
    -# = link_to omniauth_authorize_path(resource_name, provider), class: 'general-login-icon' do
    -#   %i.fa{ class: icons[provider] }
