%p.general-address
  = "#{address.first_name} #{address.last_name}"
  = yield if block_given?
  %br
  = address.address_line
  %br
  = "#{address.city} #{address.zip}"
  %br
  = address.country.name
  %br
  = t('shared.phone', phone: address.phone)